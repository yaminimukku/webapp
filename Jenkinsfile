pipeline {
    agent any
    
    stages {
        steps (code) {
            echo "coding completed"

        }
        steps (build) {
            echo "building completed"

        }
        steps (test) {
             echo "testing completed"

        }
        steps (depoly) {
            echo "deploying code"
        }
    }
}